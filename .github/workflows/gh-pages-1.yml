name: Build and deploy

on:
  push:
    branches: [mainnet_bridge_test]

jobs:
  build-ethereum-kovan:
    name: etheruem kovan build and deploy
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install and Build
        env:
          REACT_APP_ETHEREUM_RPC_URL: ${{ secrets.RPC_URL }}
        run: |
          rm -f .env
          cp .env.dev .env
          cat .env
          yarn install
          yarn build
      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@4.0.0
        with:
          branch: gh-pages
          folder: build
          repository-name: automata-network/mainnet-chainbridge-web
          ssh-key: ${{ secrets.MAINET_DEPLOY_KEY }}
